/// @description Declare variables

// speed at which text is drawn to the screen (characters per frame)
spd = 0.25;

// how many letters to draw to screen
letters = 0;

// string we will draw:
text = "This is a test string!\n";
length = string_length(text);
text_current = "";

w = 0;
h = 0;
border = 10;
border_radius = 15;
